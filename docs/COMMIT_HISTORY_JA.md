# コミット履歴の日本語メモ

GitHub のコミット一覧が英語だけだと分かりにくいため、この repo で何をしたかを日本語で残します。

| コミット | 英語の表示 | 日本語でいうと |
|---|---|---|
| `7c3e946` | Initial import for repo split | ttrss を独立 repo に分けるため最初の内容を取り込み |
| `9d4fd7a` | Add legacy data directory overrides | 旧環境のデータ保存先を指定できるよう対応 |
| `286ab53` | Attach ttrss web service to proxy network | ttrss web service を proxy network に接続 |
| `69fa844` | Update ttrss images | ttrss の Docker image を更新 |
| `2fbb8c7` | Fix ttrss postgres data directory | ttrss の PostgreSQL データ保存先を修正 |
| `89bd29c` | Capture initial TTRSS admin password | ttrss 初期 admin パスワードを取得する処理を追加 |
| `8c40092` | Persist TTRSS admin password even after first boot | 初回起動後でも ttrss admin パスワードを保存できるよう修正 |
| `17c116e` | Improve env file guidance and comments | env ファイルの説明コメントを分かりやすく改善 |

